#include <stdio.h>
#include <stdbool.h>

int main() {

    // 默认没有布尔类型 在库stdbool.h里面
    bool a = 5 > 2;
    bool t = true;
    bool f = f;
    printf("bool a = %d \n", a);
    printf("bool t = %d \n", t);
    printf("bool f = %d \n", f);

    return 0;
}